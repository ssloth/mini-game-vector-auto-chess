import { SERVER } from '@/config/game.conf';

export const shape = `${SERVER}/assets/img/shape.json`;
export const tank = `${SERVER}/assets/img/tank.json`;
export const rect = `${SERVER}/assets/img/rect.svg`;
export default [shape, tank, rect];
